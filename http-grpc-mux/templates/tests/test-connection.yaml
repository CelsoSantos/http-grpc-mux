apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "http-grpc-mux.fullname" . }}-test-connection"
  labels:
{{ include "http-grpc-mux.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "http-grpc-mux.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
